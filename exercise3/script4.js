let browser = "Firefox";


if(browser === "Edge"){
    console.log("You have got the Edge");
}
else if(browser === "Chrome" || browser === "Firefox" || browser === "Safari" || browser === "Opera"){
    console.log("Okay We support your browser");
}
else{
    console.log("We hope that this page looks okay!");
}